;;; extensions.el --- chinese-fonts Layer extensions File for Spacemacs

(setq chinese-fonts-pre-extensions
      '(
        ;; pre extension names go here
        preload
        ))

(setq chinese-fonts-post-extensions
      '(
        ;; post extension names go here
        ))

;; For each extension, define a function chinese-fonts/init-<extension-name>
;;
;; (defun chinese-fonts/init-my-extension ()
;;   "Initialize my extension"
;;   )

(defun chinese-fonts/preload ()
  "Initialize my extension"
  )

